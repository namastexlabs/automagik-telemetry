# ClickHouse Configuration
# Change these credentials for production deployments!
CLICKHOUSE_PASSWORD=telemetry_password

# Grafana Configuration
# Change the admin password for production deployments!
GF_SECURITY_ADMIN_PASSWORD=admin

# Environment
# Options: development, staging, production
ENVIRONMENT=production

# Usage Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Update the passwords in .env
# 3. If you change CLICKHOUSE_PASSWORD, also update:
#    - grafana/provisioning/datasources/clickhouse.yaml (secureJsonData.password)
# 4. Make sure .env is in .gitignore (it is by default)
# 5. Restart services: make restart
